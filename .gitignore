# — Credenciales (NUNCA subir a GitHub) —
.env
kaggle.json
*.key

# — Datos (son pesados, se regeneran con el pipeline) —
data/raw/
data/
*.duckdb
*.csv

# — Python —
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
.pytest_cache/

# — dbt —
dbt_remax/target/
dbt_remax/dbt_packages/
dbt_remax/logs/

# — Prefect —
.prefect/

# — OS —
.DS_Store
Thumbs.db
